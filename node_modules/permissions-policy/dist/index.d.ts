import { IncomingMessage, ServerResponse } from "http";
interface PermissionsPolicyOptions {
    features: Record<string, string[]>;
}
declare const _default: (options: Readonly<PermissionsPolicyOptions>) => (_req: IncomingMessage, res: ServerResponse, next: () => void) => void;
export = _default;
